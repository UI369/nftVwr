<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0">
    <title>NftVwr</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <script src="https://unpkg.com/@metamask/detect-provider/dist/detect-provider.min.js"></script>
		<link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0-beta/css/materialize.min.css">
		<link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    
    <script type="module">
    
      // This function detects most providers injected at window.ethereum
      const provider = await detectEthereumProvider();

      if (provider) {
        // From now on, this should always be true:
        // provider === window.ethereum
        

        //const ethereumButton = document.querySelector('.enableEthereumButton');
        //const showAccount = document.querySelector('.showAccount');

        //ethereumButton.addEventListener('click', () => {
          //getAccount();
        //});

        async function getAccount() {
          const accounts = await ethereum.request({ method: 'eth_requestAccounts' });
          const account = accounts[0];
          showAccount.innerHTML = account;
        }
      }
    </script>
    <style>
      body {
        color: #808080;
        font-family:Monospace;
        font-size:13px;
        text-align:center;
        background-color: #000;
        margin: 0px;
        overflow: hidden;
      }

      .container {
        width: 100%;
        height: 100%;
      }

      a {
				color: #8ff;
			}

			#menu {
				position: absolute;
				bottom: 20px;
				width: 100%;
				text-align: center;
			}

      .screenBorder {
        width: 1656px;
        height: 1024px;
        box-shadow: 0px 0px 12px rgba(0,255,255,0.5);
				border: 1px solid rgba(127,255,255,0.25);
        background-color: rgba(0,0,0,1);
				font-family: Helvetica, sans-serif;
				text-align: center;
				line-height: normal;
				cursor: default;
      }

      .overlay {
        width: 1656px;
        height: 1024px;
        box-shadow: 0px 0px 12px rgba(255,0,255,1);
				border: 1px solid rgba(255,127,255,0.25);
				font-family: Helvetica, sans-serif;
        background-color: rgba(0,0,0,1);
				text-align: center;
				line-height: normal;
				cursor: default;
      }

      .overlay .wallet_id {
        font-size: 48px;
      }

      

			.element {
				width: 128px;
				height: 192px;
				box-shadow: 0px 0px 12px rgba(0,255,255,0.5)
				font-family: Helvetica, sans-serif;
				text-align: center;
				line-height: normal;
				cursor: default;
			}

			.element:hover {
				box-shadow: 0px 0px 12px rgba(0,255,255,0.75);
				border: 5px solid rgba(127,255,255,0.75);
			}

      .element .number {
        position: absolute;
        top: 20px;
        right: 20px;
        font-size: 12px;
        color: rgba(127,255,255,0.75);
      }

      .element .symbol {
        position: absolute;
        top: 40px;
        left: 0px;
        right: 0px;
        font-size: 60px;
        font-weight: bold;
        color: rgba(255,255,255,0.75);
        text-shadow: 0 0 10px rgba(0,255,255,0.95);
      }

      .element .details {
        position: absolute;
        bottom: 15px;
        left: 0px;
        right: 0px;
        font-size: 12px;
        color: rgba(127,255,255,0.75);
      }

      .actionButton {
				width: 288px;
				height: 192px;
				font-family: Helvetica, sans-serif;
        background: transparent;
        font-size: 26px;
				text-align: center;
				line-height: normal;
				cursor: default;
			}

      .actionbutton:hover {
        cursor: pointer;
        background: rgba(0,255,255,0.5);
      }

      .outerButton {
        color: rgba(127,255,255,0.75);
        background: transparent;
        outline: 1px solid rgba(127,255,255,0.75);
        border: 0px;
        padding: 5px 10px;
        cursor: pointer;
      }

      .outerButton:hover {
        background-color: rgba(0,255,255,0.5);
      }

      .outerButton:active {
        color: #000000;
        background-color: rgba(0,255,255,0.75);
      }
    </style>
  </head>
  <body>
    <!--<header>
      <section class="container">
        <nav role="navigation">
            <span><button id="ethereumButton" class="enableEthereumButton">Enable Ethereum</button></span>
            <h2>Your Account: <span class="showAccount"></span></h2>
            <%= if function_exported?(Routes, :live_dashboard_path, 2) do %>
              <span><%= link "How?", to: Routes.live_dashboard_path(@conn, :home) %></span>
            <% end %>
          </ul>
        </nav>
        <a href="https://phoenixframework.org/" class="phx-logo">
          <img src="<%= Routes.static_path(@conn, "/images/nftvwrLogo.png") %>" alt="Phoenix Framework Logo"/>
        </a>
      </section>
    </header>-->
    
    <main role="main">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main> 
  </body>
</html>
